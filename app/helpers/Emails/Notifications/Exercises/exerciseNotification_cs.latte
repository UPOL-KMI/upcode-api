{emailSubject}Oznámení k úloze - {$exercise}{/emailSubject}
<h1>Oznámení k úloze '{$exercise}'</h1>

{if $message}
<p>
    Uživatel {$user} (<a href="mailto:{$email}">{$email}</a>) nechal zaslat všem vyučujícím,
    kteří ve svých skupinách zadali úlohu '{$exercise}', následující oznámení:
</p>
<blockquote>{$message}</blockquote>
{else}
<p>
    Uživatel {$user} (<a href="mailto:{$email}">{$email}</a>) nechal zaslat všem vyučujícím,
    kteří ve svých skupinách zadali úlohu '{$exercise}', oznámení o změně.
    Oznámení neobsahuje žádné další upřesnění.
</p>
{/if}

{button($link, 'Otevřít úlohu')|noescape}
